# Agent Workstation - 快速开始指南

## 🚀 5 分钟快速体验

### 前置条件

确保已安装：
- Python 3.10+
- Node.js 18+
- PostgreSQL
- Redis
- Ollama

### 步骤 1: 环境配置

```bash
# 1. 复制环境变量
cp .env.example .env

# 2. 编辑 .env（使用默认配置即可）
```

### 步骤 2: 启动依赖服务

**终端 1 - PostgreSQL:**
```bash
# 创建数据库
createdb agent_workstation
```

**终端 2 - Redis:**
```bash
redis-server
```

**终端 3 - Ollama:**
```bash
# 启动 Ollama
ollama serve

# 新终端下载模型
ollama pull qwen2.5:latest
```

### 步骤 3: 安装依赖

**后端:**
```bash
cd backend
pip install -r requirements.txt
```

**前端:**
```bash
cd frontend
npm install
```

### 步骤 4: 启动应用

**方式 1: 使用脚本（Windows）**
```bash
# 双击运行
start_backend.bat
start_frontend.bat
```

**方式 2: 手动启动**

终端 1 - 后端:
```bash
cd backend
python -m backend.api.main
```

终端 2 - 前端:
```bash
cd frontend
npm run dev
```

### 步骤 5: 访问应用

打开浏览器访问：http://localhost:5173

---

## 🎯 第一个任务

### 示例 1: 简单计算

在输入框输入：
```
帮我计算 (123 + 456) * 2 的结果
```

观察右侧 TODO 列表的变化。

### 示例 2: 天气查询

```
查询北京的天气
```

### 示例 3: 复杂任务

```
查询北京的天气，如果温度高于 30 度，给我发送邮件提醒
```

这个任务会触发 Human-in-the-Loop 配置表单（邮件配置）。

---

## 📊 界面说明

### 主界面布局

```
┌─────────────────────────────────────────────────────┐
│  Agent Workstation              [Connected]         │
├────────────────────────────┬────────────────────────┤
│                            │                        │
│  聊天区域                  │  任务进度面板          │
│                            │                        │
│  👤 用户: 帮我查询天气    │  Steps 2/3             │
│                            │  ████████░░ 67%        │
│  🤖 AI: 正在处理...       │                        │
│                            │  ✅ 1. 理解用户意图   │
│                            │  🔄 2. 查询天气       │
│                            │  ⏸️ 3. 返回结果       │
│                            │                        │
├────────────────────────────┴────────────────────────┤
│  [输入框] 输入任务需求...              [发送 ➤]   │
└─────────────────────────────────────────────────────┘
```

### TODO 列表状态图标

- ⏸️ **待执行**: 灰色，未开始
- 🔄 **执行中**: 蓝色旋转，正在处理
- ✅ **已完成**: 绿色对勾
- ❌ **失败**: 红色错误图标

---

## 🔧 常见问题

### 问题 1: 后端启动失败

**检查清单:**
```bash
# 1. PostgreSQL 是否启动
psql -U postgres -c "SELECT 1"

# 2. Redis 是否启动
redis-cli ping

# 3. Ollama 是否启动
curl http://localhost:11434/api/tags

# 4. 依赖是否安装
pip list | grep langchain
```

### 问题 2: 前端无法连接

**检查清单:**
- 后端是否启动：访问 http://localhost:8000/health
- 浏览器控制台是否有 CORS 错误
- WebSocket 连接状态（右上角指示器）

### 问题 3: LLM 响应缓慢

**优化建议:**
```env
# .env
LLM_MAX_TOKENS=1024  # 减少 Token 限制
LLM_TEMPERATURE=0.3  # 降低温度加快响应
```

或使用更小的模型：
```bash
ollama pull qwen2.5:7b-instruct-q4_K_M
```

---

## 📚 下一步学习

### 1. 注册自定义 Tool

编辑 `backend/tools/examples.py`:

```python
@register_tool(ToolSchema(
    name="weather_api",
    description="调用真实天气 API",
    parameters={...}
))
def weather_api(city: str):
    # 调用真实 API
    response = requests.get(f"https://api.weather.com/{city}")
    return response.json()
```

### 2. 上传领域知识

```bash
curl -X POST http://localhost:8000/api/v1/knowledge \
  -H "Content-Type: application/json" \
  -d '{
    "title": "产品手册",
    "content": "这里是产品的详细说明文档内容...",
    "metadata": {"category": "docs"}
  }'
```

### 3. 查看 API 文档

访问：http://localhost:8000/docs

### 4. 自定义前端样式

编辑 `frontend/tailwind.config.js` 修改颜色主题。

---

## 📖 深入了解

- [完整文档](README.md)
- [架构设计](ARCHITECTURE.md)
- [部署指南](DEPLOYMENT.md)

---

## 🎉 恭喜！

你已经成功启动了 Agent Workstation！

现在可以：
- 尝试更复杂的任务
- 添加自定义 Tools
- 上传领域知识
- 扩展 Agent 功能

如有问题，请查看详细文档或提交 Issue。

Happy Coding! 🚀
